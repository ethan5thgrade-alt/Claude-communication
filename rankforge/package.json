{
  "name": "rankforge",
  "version": "0.1.0",
  "description": "300-agent SEO automation system. Group 1 (Grand Orchestration) implementation.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node --import tsx --test tests/*.test.ts",
    "start": "node --import tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
