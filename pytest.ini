[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Only collect the real suite. scripts/e2e_test.py is a standalone runner whose
# helper `def test(name)` is not a pytest test and otherwise errors collection.
testpaths = tests
